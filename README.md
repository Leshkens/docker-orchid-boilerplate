### Требования
- Linux (Ubuntu, Debian)
- GNU Make
- Git
- Docker

### Установка 

1. `git clone && git remote rm origin`
2. `make init`
3. _(Опционально)_ Настроить `.env`
4. `make install`

### Использование

1. `make serve`